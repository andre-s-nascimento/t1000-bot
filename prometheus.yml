global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "t1000-bot"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8082"] # Aponta para o bot rodando na sua máquina local
